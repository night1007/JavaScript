const string = prompt("영문 소문자로 된 문자열을 입력하세요.");

// string 메서드 사용
// const firstCh = string[0].toUpperCase();    // 첫 번째 글자를 대문자로 만듬
// const remainStr = string.slice(1);          // 두 번째 글자부터 끝까지 추출하여 저장
// const result = firstCh + remainStr;         // 첫 번째 글자와 추출한 문자열을 연결
// document.write(result);